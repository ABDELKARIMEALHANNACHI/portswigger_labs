{
  "lab": "basic_origin_reflection",
  "difficulty": "apprentice",
  "cors_config": {
    "allow_origin": "REFLECT_FROM_REQUEST",
    "allow_credentials": true,
    "allow_methods": ["GET"],
    "allow_headers": ["Content-Type"]
  },
  "vulnerable_endpoint": "/accountDetails",
  "sensitive_data": ["apiKey", "email", "username"],
  "notes": "Server reads Origin header from request and reflects it back with no whitelist or validation. Combined with Allow-Credentials: true, any origin in the world can read authenticated responses."
}
